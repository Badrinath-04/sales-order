export default function AdminModulePlaceholder({ title, description }) {
  return (
    <div className="mb-12">
      <h2 className="headline mb-2 text-4xl font-extrabold tracking-tight">{title}</h2>
      <p className="font-medium text-on-surface-variant">{description}</p>
    </div>
  )
}
